/**
 * DividerBlock — visual horizontal separator.
 * content: {}
 */
import { Divider } from 'antd';

function DividerBlock() {
  return <Divider style={{ margin: '8px 0', borderColor: 'var(--border-color)' }} />;
}

export default DividerBlock;
