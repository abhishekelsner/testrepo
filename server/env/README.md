# Server environment

Copy `.env.sample` to the server root or repo root:

```bash
cp server/env/.env.sample server/.env
```

Or use a `.env` at repo root; the server loads it automatically. Never commit `.env`.
